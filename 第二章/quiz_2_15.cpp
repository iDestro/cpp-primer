/* 
 * @Author: iDestro 
 * @Description: 下面的哪个定义是不合法的？为什么？
 * (a) int ival = 1.01;
 * 对
 * (b) int &rval1 = 1.01;
 * 错，被引用的应该要是是个对象
 * (c) int &rval2 = ival;
 * 对 
 * (d) int &rval3;
 * 错，引用未初始化
 */ 

